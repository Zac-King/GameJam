using UnityEngine;
using System.Collections;

public class FixedStarCore : MonoBehaviour 
{


	// Update is called once per frame
	void Update () 
	{
		transform.rotation = new Quaternion(0,0,0,0);
	}
}
